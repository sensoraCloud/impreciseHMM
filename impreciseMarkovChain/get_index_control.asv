function [ idx ] = get_index_control( f_matrix,control )
%GET INDEX CONTROL 

N=size(f_matrix,2);

found=0;

idx=1;

while found==0,

    if sum(f_matrix(idx,:)==control)==N
        e
    end

    idx=idx+1;
end

